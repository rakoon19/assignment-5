1️⃣ What is the difference between var, let, and const?

var is global variable, you can access it from anywhere and change it
let is lexical scoped means block scoped you cant access outside of a curly braces and u can change the value
const is same as let but you cant change a constant but u can modify it if its an array or object

2️⃣ What is the spread operator (...)?

it takes only the values from array it uses to copy a variable without reference

3️⃣ What is the difference between map(), filter(), and forEach()?

map uses to do something with each element while 
filter to findout all of the matched conditions and 
forEach loop through all the element

4️⃣ What is an arrow function?

arrow function is a normal function but u dont have to write function to declare a function

function doSomething(parameter) { return //do something };
const doSomething = (parameter) => { return //do something };

5️⃣ What are template literals?

it is `` instead of '' or ""
the reason why we use it bcus we can access any variable or add conditions or call function from the string using ${}
